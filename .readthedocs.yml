version: 2

build:
  os: ubuntu-20.04
  tools:
    python: "3.7"
  apt_packages:
    - cmake
    - gcc@8

sphinx:
  configuration: docs/source/conf.py

submodules:
  include: all

python:
  version: "3.7"
  install:
    - requirements: docs/requirements.txt
    - method: setuptools
      path: .